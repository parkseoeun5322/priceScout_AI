.idea

# 가상환경 (각 PC에서 새로 생성 — requirements.txt로 재현)
.venv/

# 파이썬 캐시
__pycache__/
*.pyc
*.pyo

# 실행 중 생성되는 진행상태/임시 파일
checkpoint/
*.tmp

# 환경변수(네이버 API 키 등) — 비밀값이므로 git 추적 제외 (env.example만 공유)
.env

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# 엑셀 임시 잠금 파일 (파일을 열어두면 생성됨)
~$*